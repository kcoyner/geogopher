
sudo apt install nodejs
sudo apt install npm
sudo update-alternatives --install /usr/bin/node node /usr/bin/nodejs 1
sudo apt install nginx

cd $HOME
git clone https://github.com/geogopher/geogopher.git

sudo service nginx stop
rm -f /etc/nginx/sites-available/default
cd $HOME/geogopher
sudo cp ./nginx-server.conf  /etc/nginx/sites-available/default
sudo service nginx start


cd $HOME/geogopher
npm install

[fix nodemon]

npm run build

[ctrl c so get rid of -w for watching]

npm run server   [ change this to npm start and not watching ]

